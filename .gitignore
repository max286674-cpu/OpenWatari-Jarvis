# --- Secrets & credentials (NEVER commit) ---
.env
.env.*
!.env.example
*.session
*.session-journal
voiceprint.json
runtime_prefs.json

# --- Python / tooling ---
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Models & large binaries ---
*.onnx
*.bin
models/

# --- Runtime / generated state (machine-local, not source) ---
jarvis_jobs.sqlite
audit/
backups/
.jarvis-browser/

# --- Jarvis's private memory (personal data — keep out of git by default) ---
memory/learned/
memory/journal/

# --- OS ---
.DS_Store
Thumbs.db
