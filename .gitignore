# --- Secrets & credentials (NEVER commit) ---
.env
.env.*
!.env.example
*.session
*.session-journal
voiceprint.json
runtime_prefs.json
jarvis_session.json
jarvis_tasks.sqlite

# --- Python / tooling ---
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Models & large binaries ---
*.onnx
*.bin
models/

# --- Runtime / generated state (machine-local, not source) ---
jarvis_jobs.sqlite
audit/
backups/
.jarvis-browser/

# --- Private memory & profile (personal data — keep out of git; *.example.md templates ship) ---
memory/learned/
memory/journal/
# Personal profile files: the framework ships *.example.md templates; your filled-in copies stay local.
memory/about-you.md
memory/projects.md
memory/environment.md
memory/openclaw-fleet.md

# --- Internal working docs (kept local, not published) ---
fine-tuning.md
TODO-NOW.md
IPHONE-WATARI-CHECKLIST.md

# --- OS ---
.DS_Store
Thumbs.db

# --- Local model caches + scratch (not source) ---
.speechbrain-ecapa/
pretrained_models/
_suite_out.txt

# --- Runtime logs (edge/pc_agent — churn + may contain host/IP) ---
logs/

# Original logo/banner exports (clean copies tracked in assets/ + website/public/)
/OpenWatari*.png

# Docs site — deployed via Vercel CLI (vercel --cwd website --prod), kept out of the repo
website/
# Personal finish-line checklist (this instance, not the framework)
MY-WATARI-CHECKLIST.md
